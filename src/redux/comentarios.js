import { COMENTARIOS } from '../frombackend/comentarios';

export const Comentarios = (state = COMENTARIOS, action) => {
    switch (action.type) {

        default:
            return state;
    }
};